<?xml version="1.0" encoding="UTF-8"?>
<table>	
	<!-- 是否全部生成,默认true -->
	<all>true</all>
	<ids>	
		<column name='id'></column>
	</ids>
	<columns>
		<column name='pay_date'>
			<!-- 是否检查非空，默认false -->
			<checkEmpty>false</checkEmpty>
			<!-- 如果不为空，按该表达式进行验证,默认空 -->
			<regex></regex>
			<!-- 是否为日期，默认false  -->
			<date>true</date>
			<!-- 是否可修改，默认true  -->
			<modify>false</modify>
		</column>
		<column name='pay_date'>
			<!-- 是否检查非空，默认false -->
			<checkEmpty>false</checkEmpty>
			<!-- 如果不为空，按该表达式进行验证,默认空 -->
			<regex></regex>
			<!-- 是否为日期，默认false  -->
			<date>false</date>
			<!-- 是否可修改，默认true  -->
			<modify>false</modify>
		</column>
	</columns>
</table> 

